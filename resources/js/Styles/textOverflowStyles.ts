import { SxProps } from '@mui/system'

const textOverflowStyles: SxProps = {
  overflow: 'hidden',
  textOverflow: 'ellipsis',
  whiteSpace: 'nowrap',
  maxWidth: '100%',
}

export default textOverflowStyles
